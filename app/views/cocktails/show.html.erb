<h1>Cocktail Details</h1>
  <%= @cocktail.name %>
  <% @cocktail.doses.each do |dose| %>
  <%= dose.description %> of <%= dose.ingredient.name %>
  <%= link_to "Remove ingredient from Cocktail", dose_path(dose), method: :delete, data: {confirm: "Are you sure?"} %>
  <%end%>
 <p>
 <%= link_to "Add ingredient to Cocktail", new_cocktail_dose_path(@cocktail) %>

 </p>

 <p> <%= link_to "Back to all Cocktails", cocktails_path  %> </p>
